# Disable Spring Cloud Config for all tests
spring.cloud.config.enabled=false
spring.config.import=

# Disable Spring Cloud compatibility verifier for tests
spring.cloud.compatibility-verifier.enabled=false

